# ===========================================
# Database (PostgreSQL)
# Start with: npm run db:start (local) or use docker-compose
# ===========================================
DATABASE_URL=postgresql://testuser:testpass@localhost:5432/testdb
POSTGRES_USER=testuser
POSTGRES_PASSWORD=testpass
POSTGRES_DB=testdb

# For Docker, use postgres as host
DB_HOST=localhost
DB_PORT=5432

# ===========================================
# Shared Config
# ===========================================
NODE_ENV=development
JWT_SECRET=your-jwt-secret-change-in-production

# ===========================================
# Google OAuth (Optional - Leave empty to disable)
# Get from: https://console.cloud.google.com/apis/credentials
# Authorized redirect URI: http://localhost:3001/auth/google/callback
# If empty, the "Continue with Google" button will be hidden
# ===========================================
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_CALLBACK_URL=http://localhost:3001/auth/google/callback

# ===========================================
# Auth Service (Port 3001)
# ===========================================
PORT=3001

# ===========================================
# API Service (Port 3002)
# ===========================================
AUTH_SERVICE_URL=http://localhost:3001

# ===========================================
# Frontend (Port 3000) - Vite uses VITE_ prefix
# ===========================================
VITE_AUTH_SERVICE_URL=http://localhost:3001
VITE_API_SERVICE_URL=http://localhost:3002
VITE_GOOGLE_CLIENT_ID=
