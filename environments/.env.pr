# ============================================================================
# PR (Pull Request) ENVIRONMENT - Cloud Run (pr-{number} suffix)
# ============================================================================
# Used for: Pull request deployments, temporary environments
# Services: *-pr-{number} (e.g., api-service-pr-123)
# ============================================================================

# Google Cloud Platform
GCP_PROJECT_ID=skillful-eon-477917-b7
GCP_REGION=us-central1

# Environment
NODE_ENV=development
ENV_NAME=pr
# ENV_SUFFIX is dynamically set to -pr-{PR_NUMBER}

# Port Configuration (Cloud Run requires 8080)
PORT=8080

# Database Configuration
DATABASE_TYPE=firestore
FIREBASE_PROJECT_ID=skillful-eon-477917-b7

# Authentication
JWT_SECRET=${JWT_SECRET:-your-jwt-secret-change-in-production}
GOOGLE_CLIENT_ID=${GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${GOOGLE_CLIENT_SECRET}

# Service URLs (dynamically set during PR deployment)
# AUTH_SERVICE_URL=https://auth-service-pr-${PR_NUMBER}-${GCP_PROJECT_NUMBER}.${GCP_REGION}.run.app
# API_SERVICE_URL=https://api-service-pr-${PR_NUMBER}-${GCP_PROJECT_NUMBER}.${GCP_REGION}.run.app
# FRONTEND_URL=https://expense-tracker-pr-${PR_NUMBER}-${GCP_PROJECT_NUMBER}.${GCP_REGION}.run.app

# Frontend Variables (Vite)
VITE_USE_MOCK_API=false
