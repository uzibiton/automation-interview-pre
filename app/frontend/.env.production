# ============================================================================
# FRONTEND - PRODUCTION ENVIRONMENT (Cloud Run)
# ============================================================================
# Used for: Production deployment on Cloud Run
# ============================================================================

# Disable Mock API for production
VITE_USE_MOCK_API=false

# Service URLs (set during CI/CD deployment)
# These are placeholders - actual values injected at runtime
VITE_AUTH_SERVICE_URL=
VITE_API_SERVICE_URL=

# Google OAuth Client ID (set during CI/CD deployment)
VITE_GOOGLE_CLIENT_ID=
