# Common environment for Playwright tests
# Variables shared across environments — keep secrets out of repo if needed

# Test user credentials (shared)
TEST_USER_EMAIL=test@expenses.local
TEST_USER_PASSWORD=Test123!
TEST_USER_NAME=Test User

# Default for CI/headless runs; override per-env if needed
HEADLESS=true
