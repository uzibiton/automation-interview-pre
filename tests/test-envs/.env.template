# Environment Configuration Template
# Copy this file and update with your actual URLs

# Mechanism: Playwright loads `.env.common` first (shared values), then
# ` .env.<ENV>` (e.g. `.env.develop`, `.env.staging`) so environment-specific
# values override the common ones. Set `TEST_ENV` to select the env file.
# Example: `TEST_ENV=develop npx playwright test --config=tests/playwright.config.ts`

# Frontend URL
BASE_URL=https://your-frontend-url.run.app

# API Service URL
API_URL=https://your-api-service-url.run.app

# Auth Service URL
AUTH_URL=https://your-auth-service-url.run.app

# Test user credentials
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=testpassword123

# Optional: Additional configuration
# HEADLESS=false
# CI=false
